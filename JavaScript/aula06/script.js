/*
function soma() {
            result.value = parseInt(v1.value) + parseInt(v2.value);

            parseInt() - Transforma o tipo do elemento

        }
        
        function multiplicacao() {
            result.value = parseInt(v1.value) * parseInt(v2.value);
        }
        function divisao() {
            result.value = parseFloat(v1.value) / parseFloat(v2.value);
        }
        function subtracao(){
            result.value = parseInt(v1.value) - parseInt(v2.value);
        }
    */


function calc(){
    soma.value = parseInt(v1.value) + parseInt(v2.value);
    sub.value = parseInt(v1.value) - parseInt(v2.value);
    mult.value = parseInt(v1.value) * parseInt(v2.value);
    divi.value = parseFloat(v1.value) / parseFloat(v2.value);
}